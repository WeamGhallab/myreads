import "./App.css";

import AppRoute from "./AppRoute";
import Layout from "./layout/Layout";

const App: React.FC = () => {
  return (
    <Layout>
      <AppRoute />
    </Layout>
  );
};

export default App;
